{
  "name": "tasks",
  "version": "1.0.0",
  "private": true,
  "description": "Задачи и проекты с чатом. Без сборки и зависимостей: статические файлы, сохранение в localStorage, работает офлайн.",
  "scripts": {
    "start": "npx --yes serve .",
    "test": "node test/app.test.js && node worker/worker.test.mjs",
    "test:app": "node test/app.test.js",
    "test:worker": "node worker/worker.test.mjs"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  },
  "license": "MIT"
}
